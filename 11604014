#include <stdio.h>
#include <sys/types.h>
#include <unistd.h>
int main()
{
	int i=0;
	int j=0;
	pid_t  pid;
	do
		{
			printf("\n enter any number greater than 0"); 
  			scanf("%d", &j);	
		}
		while (j <= 0);
		pid = fork();
		if(pid==0)
